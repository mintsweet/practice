extends ../layout.pug

block content
  .transform
    i.practice(class=`pra-${type}`)
    strong #{message}
    .skip
      span#time(data-url="/") 3
      | 秒后将跳转…
append footer
  script.
    Utils.redirect();