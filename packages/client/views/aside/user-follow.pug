.block
  .content.user-follow
    .item
      span 关注了
      a(href=`/user/${info.id}/following`) #{info.following_count}
    .item
      span 关注者
      a(href=`/user/${info.id}/follower`) #{info.follower_count}
