.block
  .title 作者信息
  .content
    a.avatar(href=`/user/${author.id}`)
      img(src=author.avatar)
    a.nickname(href=`/user/${author.id}`) #{author.nickname}
    .score
      span 积分：#{author.score}
    .signature
      span “ #{author.signature || '这家伙很懒，什么都没留下'} ”
