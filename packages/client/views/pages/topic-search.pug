extends ../layout.pug

block content
  .content.page-search
    .result
      .title 搜索结果
      .statis 关于"#[strong #{q}]"的结果，共查询到#[strong #{total}]条数据
    include ../includes/topic-list.pug
  .aside
    include ../aside/topic-no-reply.pug
